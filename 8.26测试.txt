1,
2, call()是使用某个对象来调用这个函数(以列表方式传递).
   apply()跟call差不多,用数组的方式传递内容.
   bind()改变对象
3, 
4, 原型就是对象本身,原型链就是相当于复制了一份这样的原型.
5, 基本类型变量: 可以直接获取到想要内容的一种变量.
   引用变量: 这个说起来可以想象成一个内存的样子.
	     1, 如果是引用数字,它会贤惠求导的内容的中那个内容.比如 {
									a = 10;
									b = a ;
									a = 20;(给其在改变变量)
								然而引用变量在此: b只能等于10; 

								}
	     2, 如果引用的是对象,那没事正常引.

6, 
7, 闭包相当于是把函数内部的内容在函数内可以用函数调用的一种称呼.
8, 
9. http是一种网络协议.
10. get请求是把信息放在请求后边一起发出.相当于一个快递员把快递放在自行车后座去送快递.
    post请求是信息包在请求体内发出. 相当于快递员把快递放在怀里送快递.
11, node.js是一种以谷歌基于v8引擎解析代码、并优化的,以c++语言写出的可以用js代码写成服务器的一个				工具. 
   npm: 类似node.js的一个插件跟node.js一起的.

12, 创建一个    模块.
13, 同步: 是一次执行代码,前面的代码执行完之后才能执行后边的代码,这样一个顺序.
   同步操作:

    异步: 是一次性执行代码,代码各执行各的. 
   异步操作: 
14, form表单的编码方式有
15, ajax是一种可以在本页面不刷新的情况下,获取执行的内容,并显示.
    ajax可以把服务器的请求,索取到,然后在一个页面显示.
16, 静态页面时会被服务器直接访问到的.并且不需要给服务器发送请求.
    动态页面在需要服务器内部,会跟随请求一同出. (用户体验方面会相对静态页面花更多的流量.)
17, json是中个可以呈现js内容的一种程序.
      它会把js的内容,转换成字符串的形式呈现.
18,
 
19, 
20, 是一个"写的少做的多的"快捷,方便,快速实现js效果的框架.

21, git是一种非常好用的并且快捷的,安全的能够保存文件,并合并文件的一个程序.